<h1>Discounts</h1>
<% @merchant.bulk_discounts.each do |discount| %>
  <p><%=link_to discount.name, "/merchants/#{@merchant.id}/bulk_discounts/#{discount.id}" %></p>
  <p>%<%= discount.discount %></p>
  <p><%= discount.threshold %></p>
<% end %>
